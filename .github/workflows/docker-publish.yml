name: Simple Date Workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  show-date:
    runs-on: ubuntu-latest
    
    steps:
      - name: Show current date
        run: date