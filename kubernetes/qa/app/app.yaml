apiVersion: apps/v1
kind: Deployment
metadata:
  name: qa-info-deployment
  namespace: qa
  labels:
    app: qa-info
spec:
  replicas: 3
  selector:
    matchLabels:
      app: qa-info
  template:
    metadata:
      labels:
        app: qa-info
    spec:
      imagePullSecrets:
        - name: gcp-artifact-registry  # Aquí debe ir, no dentro del contenedor
      containers:
        - name: qa-info
          image: us-central1-docker.pkg.dev/elite-ceremony-476307-s9/dariasmoises979/mi-aplicacion:qa-58dba821c542d870e8f1d3704e162d55c28156b9
          ports:
            - containerPort: 5000
              name: http
              protocol: TCP
          env:
            - name: FLASK_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: qa-info-service
  namespace: qa
spec:
  selector:
    app: qa-info
  ports:
    - protocol: TCP
      port: 80          # Puerto expuesto del servicio
      targetPort: 5000  # Puerto interno del contenedor
  type: NodePort        # O LoadBalancer/ClusterIP según tu entorno
