apiVersion: apps/v1
kind: Deployment
metadata:
  name: pre-info-deployment
  namespace: pre
  labels:
    app: pre-info
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pre-info
  template:
    metadata:
      labels:
        app: pre-info
    spec:
      imagePullSecrets:
        - name: gcp-artifact-registry  # Aquí debe ir, no dentro del contenedor
      containers:
        - name: pre-info
          image: us-central1-docker.pkg.dev/elite-ceremony-476307-s9/dariasmoises979/mi-aplicacion:pre-c018ace
          ports:
            - containerPort: 5000
              name: http
              protocol: TCP
          env:
            - name: FLASK_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: pre-info-service
  namespace: pre
spec:
  selector:
    app: pre-info
  ports:
    - protocol: TCP
      port: 80          # Puerto expuesto del servicio
      targetPort: 5000  # Puerto interno del contenedor
  type: NodePort        # O LoadBalancer/ClusterIP según tu entorno
